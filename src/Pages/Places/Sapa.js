import Layout from "../../Layout";
import SapaJSON from "../../Data/Places/Sapa";

const Sapa = (props) => {
  return Layout({ page: SapaJSON, rootImgPath: "Img/Places/Sapa/" });
};

export default Sapa;
